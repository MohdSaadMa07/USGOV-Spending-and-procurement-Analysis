services:
  - type: web
    name: usgov-spending-analysis
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && npm ci --prefix ui && npm run build --prefix ui
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /
    autoDeploy: true